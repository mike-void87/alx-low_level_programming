#include "main.h"
#include <stddef.h>
/**
 * the_function - prints the binary equivalent of a decimal number
 * @w: number to print in binary
 */
void the_function(unsigned int w)
{
	int v = 0;
	int mob = 0;

	unsigned int my_ market;

	for (v = 0; >= 0; v--)
	{
		my_market = v >> i;

		if (my_market & 1)
		{
			_putchar("1");
			mob++;
		}
		else if (my_market)
			_putchar("0");
	}
	if (my_market == NULL)
		_putchar("0");
}


